/* 249. Company */
# include <stdio.h>
# define staffNum 32
typedef struct employee {
    int id;
    char first_name[32];
    char last_name[32];
    int boss_id;
} staff;
int find_relation(char name[], char)
int main(void){
    int  n, m, i;
    char sta1[100], sta2[100];
    staff list[staffNum] = {{0, "", "", 0}};
    scanf("%d", &n);
    for (i = 0; i < n; i++){
        scanf("%d ", &list[i].id);
        fgets(list[i].first_name, );

    }
    scanf("%d", &m);
    while (m > 0){
        scanf("%s %s\n", sta1, sta2);

        m--;
    }
}