/* dictionary sort */
        // char * ansPtr[ingNum], * tmpPtr;
        // for (i = 0; i < ansCnt; i++){
        //     ansPtr[i] = ans[i];
        // }
        // /* bubble sort + strcmp */
        // for (i = ansCnt - 2; i >= 0; i--){
        //     for (j = 0; j <= i; j++){
        //         if (strcmp(ansPtr[j], ansPtr[j+1]) > 0){
        //             tmpPtr = ansPtr[j];
        //             ansPtr[j] = ansPtr[j+1];
        //             ansPtr[j+1] = tmpPtr;
        //         }
        //     }
        // }